---
description: Review database migrations, models, queries, indexes, and data safety
agent: migration-reviewer
---

Review database-related CodeIgniter 4 migration work.

Check migrations, model `allowedFields` and return types, transactions, indexes, query builder usage, destructive changes, rollback, seed/backfill needs, and data safety.

Scope: $ARGUMENTS
