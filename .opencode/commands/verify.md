---
description: Run a verification checklist for recent changes
---

Verify the recent changes in this CodeIgniter 4/PHP project.

Prefer `composer test` or `vendor/bin/phpunit` when available, `php -l` for touched PHP files, and targeted `php spark` checks when relevant.

Report commands run, results, and blockers.

Scope: $ARGUMENTS
